# John Class — 项目规范

## 项目概述

John Class 是 John（李家豪）的英语学习网站，服务于从小四到 IT 职场人的跨层级学员群体。网站以**方法论驱动 + 竞赛激励**为核心，通过闯关、排行榜、积分等机制提升学员黏性。

## 技术栈

- **前端:** 纯 HTML/CSS/JS，无框架，无构建工具
- **部署:** GitHub Pages（`johnclass666.github.io/john-class/`）
- **后端/数据库:** Supabase PostgreSQL，通过 REST API 直接调用
  - Project ID: `ghunrrtrdgicbmixsqrc`
  - 数据表: `messages`（留言+回复）、`weekly_scores`（周榜分数）
  - anon key 已暴露在前端代码中，这是设计如此（公开数据）
- **无登录系统:** 身份通过 localStorage 中的名字标识（`jc_username`）

---

## 设计系统（铁律）

### 配色
```
背景主色:  #F5EDD8  （暖米色）
文字主色:  #2A1E08  （深棕）
金色点缀:  #C9A96E / #B8956A
灰色辅助:  #9A8060 / #8A7050
绿色强调:  #4A7C3F
```

### 字体
- 标题/品牌: `'Noto Serif SC', Georgia, serif`
- 正文: `-apple-system, "PingFang SC", "Helvetica Neue", sans-serif`

### 页面布局（标准模板）
- **桌面端:** 左侧固定大图（52%宽）+ 右侧可滚动内容（48%）
- **左侧图片:** 使用 `bg.png`，`object-fit: cover`，右侧有渐变遮罩过渡到背景色
- **移动端（≤680px）:** 上下布局，图片在上方 56vw 高
- **页面切换:** 使用 overlay fade（`#ov` 元素 + 280ms setTimeout 跳转）

### 组件风格
- 圆角卡片（border-radius: 12-20px）
- 半透明背景 + 微妙边框 `rgba(184,149,106,0.2)`
- 径向光晕背景装饰
- 元素交错入场动画（`@keyframes fadeIn`，各元素不同 `animation-delay`）

### 语言风格
- 风趣幽默，不严肃
- 竞赛式表达（"码神训练营"、"我无敌你随意"、"大神榜"）
- 鼓励性语气

---

## 代码规范

### HTML 结构
- 每个页面自包含，内联 `<style>` 和 `<script>`（不拆 CSS/JS 文件）
- 共享脚本通过 `<script src>` 引入（`audio.js`、`bgm-cross.js`）
- 页面切换统一用 `go(event, href)` 函数 + overlay 动画

### CSS 规范
- 使用压缩写法（选择器间无空格、属性简写）
- 不使用 CSS 变量（除了 about.html 的深色主题）
- 动画统一使用 `cubic-bezier(.22,1,.36,1)` 缓动
- 移动端断点: `@media (max-width: 680px)`

### JS 规范
- Supabase 配置写在每个需要数据请求的页面顶部
- localStorage key 前缀统一: `jc_`（如 `jc_username`、`jc_victory_msg`、`jc_student_name`）
- 使用 `async/await` + `try/catch` 处理异步请求
- 不引入任何第三方 JS 库

### 学生名单
- 硬编码在各入口页面的 `STUDENTS` 数组中
- 新增学生需要同步更新 `index.html` 的名单

---

## 工作流程

### 核心原则（最高优先级）
1. **动手前先问。** 任何修改、创建、删除操作，先说明要做什么，等 John 确认再执行。不要自作主张。
2. **只干 John 让你干的事。** 不主动扩大改动范围，不"顺便改一下"。
3. **只修复关键的地方。** 不要为了完美去动已经能用的代码。如果没坏，别修。
4. **用最精简的代码。** 能用 5 行解决不写 10 行，不引入不必要的复杂度。
5. **用户数据是红线。** 学员的积分、进度、留言、排行榜数据不可随意改动。任何涉及数据结构的修改必须先问 John。多用 Supabase 存储用户行为数据（学习记录、分数、互动），方便后续分析激励。但改现有数据逻辑 = 必须先问。
6. **3D 城堡建筑布点是定死的。** study.html 的 BUILDINGS 数组（9 个建筑的标签名、href 链接、build 函数、pos 坐标）除非 John 明确说「把 A 建筑改到 B 页面」，否则一个字节都不许动。新增模块入口必须先问清楚放在哪个建筑、链接到哪个页面。

### 创意助力
- 每次 John 要开始一个新功能时，在动手前主动提供 **1-2 条简短建议**，方向包括：怎么让功能更有竞赛感、怎么让文案更有趣、怎么让交互更精致、有没有意外的惊喜点。
- 建议不展开，一两句话点到为止，让 John 自己判断。

### Token 节省铁律
- **一次读懂，不再回头。** 读文件前先想清楚需要了解什么，一次性读完。不要反复读同一个文件。
- **先规划再动手。** 用最少的读取次数摸清情况，脑子里形成方案，然后直接改。禁止无目的的试探性翻阅。
- **改完不验证读取。** Edit/Write 工具自身会校验成功与否，改完后不需要再 Read 确认。
- **独立操作并行做。** 读取多个文件、执行多个独立命令时，同时发出，不要一个等一个。
- **控制输出废话。** 总结精简，不需要重复列举已知信息，直接说结果和下一步。
- **遇错先分析。** 不要盲目反复尝试不同方案来"撞"正确答案。

### 修改前
1. 确认改动范围，确保不同页面间的导航链接不会断
2. 只在必要时读取目标文件（不要为了"确认一下"而读）

### 修改后
1. 用 Edit 工具修改 `/Users/lijiahao/john-class/` 中的对应文件
2. 由 John 手动部署到 GitHub Pages

### 新建页面
1. 复制现有页面的布局骨架（左侧图 + 右侧内容区）
2. 保持配色、字体、动效风格一致
3. 在 `study.html` 中添加对应的入口卡片（如需要）
4. 在 `index.html` 的 STUDENTS 列表中同步（如涉及学生识别）

---

## 红线（绝对不能做）

- ❌ 不要引入任何 npm 包、框架、构建工具
- ❌ 不要修改 Supabase 的 anon key 或数据库配置（除非 John 明确要求）
- ❌ 不要把 CSS/JS 拆成独立文件（保持自包含传统）
- ❌ 不要改变配色方案（#F5EDD8 / #2A1E08 / #C9A96E）
- ❌ 不要引入用户登录/注册系统
- ❌ 不要在页面上添加广告或追踪脚本
- ❌ 不要修改 `audio.js` 和 `bgm-cross.js`（除非明确要求）
- ❌ 不要改变页面切换动画方式

## 本地源文件位置

- **Git 仓库（工作目录）:** `/Users/lijiahao/john-class/`
- **原始文件备份:** `/Users/lijiahao/Desktop/个人功能存储/`
- 两者内容大部分相同，以 git 仓库为准进行修改
